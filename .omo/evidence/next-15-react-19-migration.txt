# Next 15 + React 19 migration evidence

- Task ID: `T-1c97c1a2-cce0-40de-b752-073a9256a80e`
- Collected: `2026-07-22T17:02:31+02:00`
- Model: `9router/default` — selected by session configuration.

## Changes verified

- Upgraded Next.js to `15.5.21` and React / React DOM to `19.2.8`.
- Migrated Next 15 asynchronous `headers()` and dynamic route `params` access.
- Removed archived `next-compose-plugins`; kept direct bundle-analyzer configuration.
- Eliminated React 19 reduced-motion hydration divergence using root `MotionConfig reducedMotion="user"` and static Motion props.

## Commands and results

```sh
pnpm build
```

Pass. Next.js `15.5.21`; compilation and type validation passed; 47 static pages generated.

```sh
pnpm test -- components/PageTransition/PageTransition.test.tsx components/HeroStagger/HeroStagger.test.tsx components/CardMotionWrapper/CardMotionWrapper.test.tsx lib/sendEmail.test.tsx
```

Pass. 4 suites, 19 tests.

```sh
pnpm prettier
```

Pass. All matched files formatted.

```sh
pnpm lint
```

Pass with pre-existing warnings only. Next 16 deprecation warning for `next lint` reported.

```sh
pnpm exec playwright test e2e/interactions.spec.ts --project=chromium --workers=1
```

Migration hydration behavior passed: reduced-motion navigation and no unexpected browser console errors. Command remains nonzero because Roman-redesign baseline expects `rgb(147, 197, 253)` focus color but rendered `rgb(248, 164, 140)`.

## Known environmental / unrelated limits

- Full E2E cannot run Firefox: executable missing at `/home/tom/.cache/ms-playwright/firefox-1532/firefox/firefox`.
- Chromium full-suite visual and a11y failures predate migration scope and track stale Roman-redesign expectations.
- TypeScript LSP unavailable; installation was previously declined. `pnpm build` performed TypeScript validation.

## Freshness and reproduction

Run commands from repository root after `pnpm install --frozen-lockfile`. No secret inputs needed. Token usage unavailable from runtime telemetry.

## Task 5 E2E baseline and accessibility repair

- Collected: `2026-07-22T17:34:53+02:00`
- Model: `9router/default` — selected by session configuration.
- Roman token values replaced obsolete blue/default E2E assertions.
- `foreground.muted.DEFAULT` changed from `#78716C` to `#706963`: `#706963` on `#F5F0E8` has 4.76:1 contrast, clearing WCAG AA body-text threshold.
- Primary CTA uses warm foreground on `accent.light`; secondary CTA uses contrast-safe `text-accent` in light mode and `text-accent-light` in dark mode. Axe no longer reports homepage contrast violations.
- Axe scan waits for `main` page-transition opacity `1`; avoids scanning transition-composited text.
- Playwright runs Chromium serially: local `next dev` failed concurrent first-visit compilation; Firefox and WebKit binaries absent from local cache.

### Reproduced commands

```sh
pnpm test
pnpm lint
pnpm prettier
unset PNPM_HOME npm_config_prefix PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH && export PLAYWRIGHT_BROWSERS_PATH="$HOME/.cache/ms-playwright" && pnpm e2e:headless
pnpm build
```

### Results

- `pnpm test`: pass — 15 suites, 91 tests.
- `pnpm lint`: pass — existing import-order warning in `lib/sendEmail.tsx` only.
- `pnpm prettier`: pass.
- `pnpm e2e:headless`: pass — 40 Chromium tests in 1.9m.
- `pnpm build`: pass — static and SSG routes generated.
- TypeScript LSP unavailable; user previously declined server installation. Production build performed TypeScript validation.
