====================================================================
EVIDENCE FILE: Task 2 — Nix Flake devShell Setup (2026 Portfolio Overhaul)
====================================================================
Timestamp: 2026-07-19
Status: COMPLETE — flake.nix, flake.lock, .envrc all created and verified
Agent: Sisyphus-Junior

====================================================================
TASK SUMMARY
====================================================================

Created reproducible Nix flake devShell providing nodejs_22, pnpm,
and git for the project. Added Playwright Chromium runtime libraries
in a later refinement (Task 19 runtime restoration).

====================================================================
FILES CREATED / MODIFIED
====================================================================

1. flake.nix — Nix flake with devShell
   - nixpkgs channel: github:NixOS/nixpkgs/nixos-unstable (2026-07-18)
   - Packages: nodejs_22, pnpm, git
   - Chromium runtime libs added later (ALSA, Cairo, CUPS, D-Bus,
     Expat, Fontconfig, Freetype, GTK 3, libdrm, libGL,
     X11/XCB/Xext/XKBCommon, NSPR/NSS, Pango)

2. flake.lock — generated by `nix flake lock`

3. .envrc — `use flake` directive for direnv

====================================================================
VERIFICATION
====================================================================

1. flake.nix exists and is syntactically valid:

   Command: ls -la flake.nix
   Result: file exists, tracked in git
   Exit code: 0

2. flake.lock generated:

   Command: nix flake lock
   Result: flake.lock generated successfully
   Exit code: 0

3. Flake check passes:

   Command: nix flake check
   Result: OK
   Exit code: 0

4. direnv activation succeeds:

   Command: direnv allow
   Result: devShell loaded
   Exit code: 0

5. Node version resolved:

   Command: nix develop --command node --version
   Result: v22.23.1 ✓

6. pnpm version resolved:

   Command: nix develop --command bash -c 'unset PNPM_HOME npm_config_prefix; pnpm --version'
   Result: 11.13.1 ✓
   Note: pnpm binary is at /nix/store/.../pnpm-11.13.1/bin/pnpm
         Nix devShell sets PNPM_HOME=null which crashes pnpm 11;
         must `unset PNPM_HOME npm_config_prefix` before pnpm commands.

====================================================================
ISSUES / GOTCHAS
====================================================================

1. pnpm in Nix vs system version mismatch:
   - Nix flake provides pnpm v11.13.1
   - System pnpm is v10.15.1
   - System pnpm (v10) used for package management due to
     pnpm 11 null-path crash in Nix devShell

2. pnpm --version FAILS in project dir:
   - `package.json` preinstall hook `npm-only-allow --PM yarn`
     blocked pnpm execution. Resolved in Todo 4 (pnpm migration).

3. Git tracking required:
   - `nix flake lock` requires flake files to be git-tracked.
     `git add flake.nix` is prerequisite.

====================================================================
EVIDENCE STATUS
====================================================================

[✓] flake.nix created and git-tracked
[✓] flake.lock generated
[✓] .envrc created with `use flake`
[✓] nix flake check passes (EXIT_CODE=0)
[✓] direnv allow succeeds
[✓] nodejs v22.23.1 available in devShell
[✓] pnpm v11.13.1 available in devShell
[✓] Chromium runtime libraries added (Task 19 refinement)

====================================================================
